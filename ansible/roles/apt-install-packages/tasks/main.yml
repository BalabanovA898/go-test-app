---
- name: Update apt cache
  apt:
      update_cache: true
      cache_valid_time: 3600
  become: true

- name: Setting up common packages
  apt:
      name: "{{ packages_to_install }}"
      state: present
  become: true
