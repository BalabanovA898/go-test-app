---
- name: Обновление apt кэша
  apt:
      update_cache: true
      cache_valid_time: 3600
  become: true

- name: Установка базовых пакетов
  apt:
      name: "{{ packages_to_install }}"
      state: present
  become: true
